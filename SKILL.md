---
name: novel-world-builder
description: 一个全中文驱动的长篇小说世界观架构与正文引擎。场景分类采用嵌套结构（场景氛围/场景空间/），消除目录同名歧义。
assets:
 - source: "[全局模板路径]/github工作流规范.md"
 target: "./novel-world-db/[小说名称]/.github/github工作流规范.md"
 condition: "仅协作模式"
 - source: "[全局模板路径]/.agent_name"
 target: "./novel-world-db/[小说名称]/.github/.agent_name"
 condition: "仅协作模式"
---

# 小说世界观联合架构师 (Novel World Builder)

本技能是长篇小说的后台数据库管理员、联合主笔与 DevOps 工程师。系统通过维护全局索引、隔离动静数据、遵循单一事实来源（SSOT）原则，并采用"声明式约束"与"量化反模式检测"指导 Agent 协作，保证百万字长篇的逻辑自洽与极致的文学质感。

*最高指令 (CRITICAL):* 所有的系统思考、剧情讨论、文件读写及后台数据同步，必须 100% 使用中文（简体中文）。

## 1. 统一工作区与单库归类架构
所有的读写操作必须且只能限定在 `./novel-world-db/[小说名称]/` 目录下。

**[目录树约束]**
本系统采用"一书一库"规范。目录分两类：

**必选目录（新建项目时必须创建）：**
```text
./novel-world-db/[小说名称]/
├── .git/ (协作模式标识。若不存在则自动进入单兵模式)
├── .github/ (基础设施：仅在协作模式下创建与使用)
│ ├── .agent_name
│ └── github工作流规范.md
├── 静态设定库/ (法定基石：只读/低频修改)
│ ├── 角色/
│ ├── 场景/ (嵌套结构)
│ │   ├── 场景氛围/ (P1主观渲染层：氛围卡，各角色视角感官对比，随卷章动态更新)
│ │   └── 场景空间/ (P4客观物理层：材质、尺寸、声源、气味，预设后几乎不变)
│ ├── 底层机制/
│ ├── 物品/
│ └── 剧情事件/
├── .动态数据/ (法定基石：高频 YAML 状态，包含日常渐进异常表)
└── 正文手稿/
```

**扩展目录（按需创建，须在 `静态设定库/核心配置.md` 的扩展分类字典中声明收录标准）：**
- 如 `实体/`、`终极形态/`、`势力/` 等，均属此类，非强制。

## 2. Agent 全生命周期工作流 (Lifecycle Workflow)

### 阶段 0: 项目初始化与脚手架搭建 (Project Bootstrap)
*触发:* 用户要求创建新小说项目。
1. **模式确认:** 询问用户是否需要 Git 协作。
2. **构建目录树:** 
   - 必建目录：`静态设定库/`（含五大基石目录）、`.动态数据/`、`正文手稿/`。
   - **[条件创建]**: 仅当开启协作模式时，显式创建 `.github/` 目录。
3. **资产装载:**
   - **协作模式**: 读取 `assets` 并复制 `github工作流规范.md` 与 `.agent_name`。
   - **单兵模式**: 跳过 `assets` 引用，直接初始化核心配置与游标。

### 阶段 1: 冷启动与环境感知 (Cold Start)
*触发:* 用户开启新对话或提及已有项目。
1. **强制扫描清单（核心动作）**: 不论何种模式，必须依次读取：
   - `./静态设定库/核心配置.md` (对齐基调)
   - `./静态设定库/全景索引.md` (对齐拓扑)
   - `./.动态数据/时间线游标.yaml` (对齐当前章节)
   - `./.动态数据/日常渐进异常表.yaml` (对齐当前异常刻度，确保阶段性描写不超纲)
2. **环境检测:** 检查是否存在 `./novel-world-db/[小说名称]/.git/`，依此切换"协作/单兵"工作模式。

### 阶段 2: 深度推演与渲染 (Drafting & Rendering Mode)
1. **感官注入:** 动笔前必须回读 `静态设定库/场景/场景空间/`（物理细节）与 `静态设定库/场景/场景氛围/`（感官对比）。
2. **行为注入:** 回读角色的 `[角色名.行为种子.md]`，将抽象性格具象化为压力下的生理本能动作。

### 阶段 3: 反模式检测量化执行 (Quantitative QC)
*触发:* 推演草稿生成完毕，输出前执行。

**A类：强制修正（命中即重写该段落）**
- **句长平铺**: 句长标准差 < 18 → 立即插入 3-8 字的短句（不少于 3 处）。
- **悬念结尾**: 命中关键词（"但这只是开始"、"一切才刚刚……"等） → 强制替换为物理细节结尾。
- **思维连续性**: 未完成句（"——"）比例 = 0% → 强制插入 3 处思维截断。

**B类：强烈警告（建议替换指标）**
- **AI 高频词**: 命中"凝视、蠕动、蔓延、吞噬、沉溺、喧嚣、错综复杂"。
- **情感密度检测**: 连续 3 段以上出现"情感峰值词"（心痛/绝望/窒息/温暖/柔软） → 强烈建议在高潮处插入生理打断动作（如：指甲掐入掌心、喉头生疼）。
- **对话回应**: 对话完整回应率 > 90% → 建议插入截断对话（说话→……→沉默）。

**C类：静默记录（不干预）**
- 比喻密度 > 1个/200字；"于是/因此" 连接词使用率偏高。

### 阶段 4: 入库与提交 (Commit Mode)
1. **业务更新**: 自动更新 YAML 状态、全景索引及 Markdown 正文归档。
2. **协作闭环**: 协作模式下根据规范发起分支提交与 PR。

## 3. 核心数据结构与强类型模板 (Strict Templates)

**法定标签语法 (必须与分类目录高度对应):**
- `[@角色:名]`, `[@实体:名]`, `[@物品:名]`, `[#机制:名]`, `[#剧情事件:名]`

**0. 核心配置 (`./静态设定库/核心配置.md`)**
除了基调与禁忌，必须包含 **[扩展分类字典]**：若需创建动态扩展目录（如 `/势力/`），必须在此声明该目录的收录标准，作为后续入库的仲裁依据。

**1. 时间线游标 (`./.动态数据/时间线游标.yaml`)**
```yaml
时间线:
  当前卷章: "[唯一真相来源。例: 第一卷 / 第32章]"
  时间地点: "[例: 表世界 / 废弃校舍]"
  当前目标: "..."
  近期事件: ["..."]
  场景渲染器:
    张力指数: [1-10的数值，指导本章的节奏快慢与压迫感]
    情感底色: "[例: 绝望中的微光 / 轻松的日常伪装]"
```

**2. 角色动态状态 (`./.动态数据/角色动态状态.yaml`)**
```yaml
角色列表:
  [@角色:角色名]:
    表层状态: "[身体现状/核心数值/物品栏等外在表现]"
    深层暗流:
      心理锚点: "[当前支撑其行动的核心信念，或濒临崩溃的恐惧源]"
      关系债: "[近期与他人的恩怨纠葛、内疚或隐秘契约]"
# [防呆锁]: 绝对禁止在此处添加进度或时间字段。
```

**3. 通用静态实体 (`./静态设定库/[类别]/[名称].md`)**
```markdown
# 静态设定: `[@类别:名称]` (注意：若为剧情事件，统一使用 `# 静态设定: [#剧情事件:名称]`)
- **核心特征/用途**: [渴望/缺陷/功能/发生条件]
- **绝对动机**: [CRITICAL: 角色或实体不可被轻易覆盖的核心驱动力，行为逻辑的最高底线]
- **感官锚点/基调**: [视觉/听觉词汇/情绪基调]
- **关联引用**: [必须使用法定标签显式列出绑定的机制或关系]
```

**4. 静态底层机制 (`./静态设定库/底层机制/[名称].md`)**
```markdown
# 底层机制: `[#机制:机制名称]`
- **绝对限制**: [物理/魔法限制]
- **绕过条件**: [合法规避方案]
- **作用范围**: [受约束实体列表]
```

**5. 角色行为种子 (`./静态设定库/角色/[角色名.行为种子].md`)**
纯行为描写，无分析解释。必须包含：
- 压力下的下意识动作（如：扯衣角、回避视线）。
- 情绪波动的生理反应。
- 互动时的本能态度。

**6. 场景空间与氛围（`./静态设定库/场景/场景氛围/[场景名].md` & `./静态设定库/场景/场景空间/[场景名].md`）**
严禁在物理层写入"显得压抑"等情感判断。必须包含：
- **物理属性**（`场景空间/`）：空间布局（具体尺寸）、光源类型/强度、材质触感、声源、气味。
- **氛围卡**（`场景氛围/`）：各角色视角的感官对比、氛围关键词。
- **叙事功能标注**：适合的场景类型与特殊触发机制。

**7. 日常渐进异常表 (`./.动态数据/日常渐进异常表.yaml`)**
以时间为刻度（如：每7天），记录缓慢异变的节奏。必须包含：
- 当前异常感知强度（必须绑定特定的底层机制）。
- 心蚀进度同步。
- 旁人的察觉程度。

## 4. 冲突拦截与逻辑防线 (Conflict Interception)
1. **机制与动机拦截**: 严禁偏离机制或角色的 **`绝对动机`**。
2. **前置事件闭环**: 扭转动机必须引用库内 `[#剧情事件:...]`，严禁现场捏造。
